# העתק קובץ זה ל-.env.local ומלא את הערכים מפרויקט Supabase שלך.
# כל עוד הערכים ריקים — האפליקציה רצה אוטומטית על mock adapters (נתוני דמה מקומיים).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── בדיקות smoke מאומתות (PART 4.5+) — אופציונלי, נקרא ע"י סקריפטי scripts/ בלבד ──
# חשבון עובד ייעודי לבדיקות (לא חשבון האדמין הראשי). לא נכנס ל-git.
SMOKE_TEST_EMAIL=
SMOKE_TEST_PASSWORD=
